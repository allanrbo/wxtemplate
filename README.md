# mywxapp1 Template

Minimal wxWidgets application template that opens a hello world window. The build system automatically fetches and statically links wxWidgets so the resulting binary has no external GUI dependencies beyond the toolkits already present on the system.

See `README.DEVELOPER.md` for build instructions.

Icon sources and conversion scripts are under `graphics/appicon/`.

